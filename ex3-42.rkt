;; The solutions are basically the same, so both are valid.
;; Bet Bitdiddle's solution doesn't change the behaviour from a concurrency
;; standpoint. The only practical difference is that the same serializer is used
;; every time (one for 'withdraw and one for 'deposit)
